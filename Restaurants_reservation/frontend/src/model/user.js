export class User{

    constructor(userID,username,password,name) {
            this.userID = userID;
            this.username = username;
            this.password  = password;
            this.name = name;
    }
}